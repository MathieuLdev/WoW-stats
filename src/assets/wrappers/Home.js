import styled from "styled-components";

const Wrapper = styled.p`
	font-size: 2rem;

	.home-container {
		position: absolute;
		width: 100%;
		height: 100vh;
	}

	.wow-logo {
		display: block;
		margin: 20px auto;
	}
`;

export default Wrapper;
